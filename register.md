// register form

=> what server accept
	{
		username,
		password,
		confirmPassword,
		gender
	}
<= what i send to frontend
	error: if i found any error in data u send
	message: if evertyhing is correct
